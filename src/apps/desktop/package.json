{
  "name": "@arkloop/desktop",
  "productName": "Arkloop",
  "version": "26.3.27",
  "private": true,
  "description": "Arkloop desktop app",
  "homepage": "https://github.com/qqqqqf-q/Arkloop",
  "author": {
    "name": "Arkloop",
    "email": "security@arkloop.cn"
  },
  "packageManager": "pnpm@10.29.3",
  "main": "dist/main/index.js",
  "scripts": {
    "dev": "node scripts/dev.mjs",
    "dev:electron": "node scripts/dev.mjs",
    "preview:mac": "node scripts/preview-mac-app.mjs",
    "build": "pnpm build:web && pnpm build:electron && pnpm build:sidecar",
    "build:web": "node scripts/build-web-for-desktop.mjs",
    "build:electron": "tsc -p tsconfig.main.json && tsc -p tsconfig.preload.json",
    "build:sidecar": "node scripts/build-sidecar.mjs",
    "build:sidecar:all": "node scripts/build-sidecar.mjs --all",
    "pack:app": "pnpm build && electron-builder --dir",
    "dist": "pnpm build && electron-builder",
    "dist:mac": "pnpm build:web && pnpm build:electron && node scripts/build-sidecar.mjs --platform darwin --arch arm64 && node scripts/build-sidecar.mjs --platform darwin --arch x64 && electron-builder --mac",
    "dist:mac:arm64": "pnpm build:web && pnpm build:electron && node scripts/build-sidecar.mjs --platform darwin --arch arm64 && electron-builder --mac --arm64",
    "dist:mac:x64": "pnpm build:web && pnpm build:electron && node scripts/build-sidecar.mjs --platform darwin --arch x64 && electron-builder --mac --x64",
    "dist:linux": "pnpm build:web && pnpm build:electron && pnpm build:sidecar && electron-builder --linux",
    "dist:win": "pnpm build:web && pnpm build:electron && node scripts/build-sidecar.mjs --platform win32 --arch x64 && electron-builder --win",
    "dist:all": "pnpm build:web && pnpm build:electron && pnpm build:sidecar:all && electron-builder -mwl",
    "type-check": "tsc -p tsconfig.main.json --noEmit && tsc -p tsconfig.preload.json --noEmit"
  },
  "dependencies": {
    "electron-updater": "^6.6.2"
  },
  "devDependencies": {
    "@types/node": "^22.16.0",
    "electron": "^41.0.3",
    "electron-builder": "^26.0.12",
    "typescript": "~5.9.3"
  }
}
