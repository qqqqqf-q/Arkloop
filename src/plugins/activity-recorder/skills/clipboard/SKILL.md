# Clipboard

Use Clipboard when the user refers to copied content, asks what is on the clipboard, or wants structured clipboard data read without pasting it into chat.

Prefer the Clipboard MCP server. Use `clipboard_paste` for reads and `clipboard_copy` for writes.

For normal activity recall, only read the clipboard when it is clearly relevant to the user's request.
